# 交接说明 · 薄荷镇剧本杀部署

> 本文档记录到目前为止做了什么、得到什么结论、接下来该怎么做。
> 仓库：https://github.com/sunandsong/jubensha

---

## 一、这是什么

一个 5 人线上剧本杀《薄荷镇的最后一夜》：主持人建房 → 玩家凭 4 位房号进房 →
服务端随机分配角色 → 每人只看到自己的剧本（含独家秘密/目标）→ 主持人按幕推进、逐步解锁线索。

仓库里有**三套等价实现**（前端逻辑一致，只是托管平台/后端不同）：

| 目录 | 平台 | 后端 | 状态存储 |
|------|------|------|----------|
| `worker/` + `public/` | Cloudflare Workers | Worker + Durable Object | DO storage |
| `edgeone/` | EdgeOne Pages | 边缘函数 | Blob |
| `miniprogram-jubensha/` | 微信小程序 + 云开发 | 云函数 game | 云数据库 rooms |

---

## 二、已经部署的两个网页版（都已上线，但大陆都打不开）

### EdgeOne Pages
- 地址：https://bohe-jubensha-mggnokgp.edgeone.cool
- 账号：腾讯云 sunandsong123@gmail.com（账号 ID 100006714020）
- 项目 ID：makers-m9schhpmljkj
- 后端用 Blob 存储，**海外实测全流程通过**（建房/进房/分配/拉角色/控场）
- ❌ **中国大陆直连 401**：项目在"全球(不含大陆)"区域，默认域名 `*.edgeone.cool` 无法备案，进不了大陆

### Cloudflare Workers
- 地址：https://bohe-jubensha.sunandsong123.workers.dev
- 账号：Cloudflare sunandsong123@gmail.com（账号 ID 4805923088c0b455efff3d768a822b19）
- 一个 Worker 同时托管前端 + `/api`（`worker/wrangler.toml` 里加了 `[assets]`）
- ❌ **中国大陆被墙**：`*.workers.dev` 在大陆时通时不通，裸网测试打不开

---

## 三、核心结论（重要）

**大陆玩家 + 免费 + 免备案，这三个条件在"网页方案"上凑不齐。** 原因：

- 国内任何平台（EdgeOne/阿里云/腾讯云）面向大陆提供网页访问，**域名必须 ICP 备案**；免费默认域名无法备案。
- 海外平台（Cloudflare workers.dev、Vercel 等）在大陆**被墙或不稳定**。
- 有域名但**没备案**一样不行（会被备案检查拦）。

➡️ **唯一同时满足"大陆能用 + 免费 + 免备案"的方案：微信小程序。**
小程序跑在微信里不需要网站备案，微信云开发有免费额度，给朋友玩用"体验版"无需审核。

**决定：走微信小程序版（`miniprogram-jubensha/`），放弃两个网页版。**

---

## 四、接下来要做的（按顺序）

详细图文步骤见 `部署到微信小程序.md`，这里是清单：

### 1. 注册小程序账号（当前唯一卡点）
- 打开 https://mp.weixin.qq.com → 右上角「立即注册」→ **选「小程序」**（不是公众号！）
- 用一个**从没注册过微信公众平台的邮箱**（已被公众号占用的邮箱不行；没有就新申一个 QQ/163 邮箱）
- 邮箱激活 → 主体选「个人」→ 微信扫码做管理员实名
- 进后台「开发管理 → 开发设置」拿到 **AppID**

> ⚠️ 之前扫码登录看到"一堆号让选"，那些都是**公众号**，不是小程序——所以选不出来。必须按上面**新注册一个小程序**。

### 2. 本地导入 + 配置
- 下载安装「微信开发者工具」
- 「导入项目」→ 选目录 `miniprogram-jubensha/` → 填上一步的 AppID
- 打开 `project.config.json`，把 `"appid": "你的小程序AppID"` 改成真实 AppID

### 3. 开通云开发 + 建数据库
- 开发者工具点「云开发」→ 开通（选**免费版**）
- 云开发控制台「数据库」→ 新建集合，集合名填 **`rooms`**

### 4. 部署云函数
- 左侧文件树右键 `cloudfunctions/game` → 「上传并部署：云端安装依赖」

### 5. 玩
- 点「编译」即可在模拟器试玩
- 给朋友玩：「上传」一个版本 → mp 后台设为**体验版** → 「体验成员」里加朋友微信号（**不用是好友，填微信号即可，免审核**）

---

## 五、代码改本子

改对应文件里的 `ROLES / STAGES / CLUES`，角色数量决定房间人数：
- 小程序版：`miniprogram-jubensha/cloudfunctions/game/index.js`（改完要重新上传部署云函数）
- 三套数据内容完全一致

---

## 六、Git

- 远程：https://github.com/sunandsong/jubensha （已全部推送，工作区干净）
- 本地路径：`/Users/zhangsong/Desktop/bohe/files/bohe`
- 提交历史最新到"微信小程序版 + Cloudflare assets"

---

## 七、一句话给接手的人

> 网页版（EdgeOne / Cloudflare）大陆都打不开，已确认是墙+备案的硬限制，别再在网页上折腾。
> 直接做**微信小程序版**：代码在 `miniprogram-jubensha/`，照着 `部署到微信小程序.md` 走，
> 当前唯一卡点是**注册一个新的小程序账号（要用没用过的邮箱、选"小程序"而不是公众号）**。
> 注册出 AppID 后，导入项目 → 开通云开发 → 建 rooms 集合 → 部署 game 云函数 → 体验版加朋友即玩。
