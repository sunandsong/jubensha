# 部署到微信小程序（免费 · 免备案 · 大陆可用）

大陆玩家、没有备案域名、又不想花钱时，唯一可行的路：做成**微信小程序 + 微信云开发**。
小程序跑在微信里无需网站备案，云开发有免费额度，玩家在群里点开即玩。

```
miniprogram-jubensha/
├── project.config.json          # 工程配置（需填你的小程序 AppID）
├── miniprogram/                 # 前端
│   ├── app.js / app.json / app.wxss / sitemap.json
│   └── pages/index/             # 唯一页面：入口 / 主持人 / 玩家 三态切换
└── cloudfunctions/
    └── game/                    # 云函数：处理所有动作，房间状态存云数据库
        ├── index.js
        └── package.json
```

与网页版的区别：
- 后端从 HTTP 接口改成**一个云函数 `game`**（`event.action` 分发）。
- 玩家身份用微信 **OPENID**（云函数上下文取，前端无法伪造），不再自发 token；分配/控场只有**建房者（主持人）**能操作。
- 房间状态存**云数据库 `rooms` 集合**（文档 `_id` = 4 位房号）。

---

## 一、准备（一次性）

1. **注册小程序账号**：[mp.weixin.qq.com](https://mp.weixin.qq.com) → 立即注册 → 选「小程序」→ 个人主体（用你已实名的微信即可）。注册后在「开发管理 → 开发设置」里拿到 **AppID**。
2. **下载微信开发者工具**：[下载地址](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)，安装后用微信扫码登录。

## 二、导入项目

1. 打开微信开发者工具 →「导入项目」。
2. 目录选 `miniprogram-jubensha/`，AppID 填你自己的（或先用「测试号」）。
3. 打开 `project.config.json`，把 `"appid": "你的小程序AppID"` 换成真实 AppID。

## 三、开通云开发

1. 工具顶部点 **「云开发」** 按钮 → 开通（选**免费基础版**）。会自动创建一个环境，记下环境 ID（代码里用的是 `DYNAMIC_CURRENT_ENV`，单环境无需手填）。
2. 进入「云开发控制台 → 数据库」→ **新建集合**，集合名填 **`rooms`**（权限选「仅创建者可读写」即可，云函数有管理员权限不受限）。

## 四、部署云函数

1. 在工具左侧文件树里，右键 `cloudfunctions/game` 文件夹 → **「上传并部署：云端安装依赖」**。
2. 等待部署完成（首次会装 `wx-server-sdk`，约 1 分钟）。

## 五、跑起来

1. 点工具上的 **「编译」**，模拟器里就能玩。
2. 真机：点 **「预览」** 扫码，或 **「真机调试」**。
3. 给朋友玩：点 **「上传」** 上传一个版本 → 在 [mp.weixin.qq.com](https://mp.weixin.qq.com) 后台「版本管理」把它设为**体验版** → 在「成员管理 → 体验成员」里加上朋友的微信号。**体验版无需审核**，加进来的朋友就能在自己微信里打开玩。

> 想让任何人都能搜到/打开，则需「提交审核 → 发布」，个人小程序也支持，审核约 1–2 天。

## 玩法

主持人在入口**选一个本**建房 → 拿到 4 位房号发群里 → 朋友进小程序输房号 + 昵称加入 →
满员后主持人「随机分配角色」→ 每人只看到自己的剧本（含秘密/目标）→ 主持人按幕推进、线索逐步解锁。
当前内置 3 个本：**薄荷镇的最后一夜 / 末班地铁 / 杀青夜**（均 5 人）。

## 加本子 / 换本子

剧本都在 `cloudfunctions/game/index.js` 顶部的 **`SCRIPTS` 剧本库**里。加新本就在 `SCRIPTS` 里加一项：

```js
const SCRIPTS = {
  bohe: { title, intro, roles:[...], stages:[...], clues:[...] },
  你的新本id: { title:"标题", intro:"一句话简介", roles:[/* 角色，数量=人数 */], stages:[/* 幕 */], clues:[/* 线索 */] },
};
```

每个角色含 `id / name / title / av / public / scene{t,b} / secret / mission`；`stages[i].clues` 是该幕已解锁的线索下标。
改完**重新上传部署云函数**即可，前端会自动在入口列出新本，无需改前端。

## 排错

- 调用云函数报 `cloud init` 相关错误：确认已开通云开发、且 `app.js` 里 `wx.cloud.init` 正常执行。
- 报数据库集合不存在：去云开发控制台建 `rooms` 集合。
- 进房/状态不更新：前端每 1.5 秒轮询一次 `state`，注意真机网络。
- 免费额度：云函数调用次数/数据库读写有每月免费配额，5 人小局完全够用。
