# 薄荷镇剧本杀 · Cloudflare 部署指南

一个纯 H5 剧本杀工具：主持人建房 → 玩家进房 → 服务端随机分配角色 → 每人只看自己的剧本 → 主持人按幕推进、线索同步解锁。

后端用 Cloudflare Workers + Durable Objects（免费计划即可），前端用 Cloudflare Pages（免费）。**不需要服务器，不需要数据库，不需要备案**（用 Cloudflare 自带的 `.workers.dev` / `.pages.dev` 域名）。

---

## 目录结构

```
bohe/
├── worker/                  后端
│   ├── src/
│   │   ├── index.js         Worker 入口（路由）
│   │   ├── room.js          房间逻辑（Durable Object，洗牌、发角色）
│   │   └── script.js        本子数据（改剧本就改这里）
│   ├── wrangler.toml        Cloudflare 配置
│   └── package.json
├── public/                  前端
│   └── index.html           主持人端 + 玩家端（单页）
└── 部署指南.md               本文件
```

---

## 准备工作（一次性）

### 1. 装 Node.js
去 https://nodejs.org 下载 LTS 版安装。装完在终端确认：
```
node -v      # 出现版本号即可，需 v18 以上
npm -v
```

### 2. 注册 Cloudflare 账号
https://dash.cloudflare.com/sign-up 免费注册，邮箱验证一下即可。

---

## 第一步：部署后端 Worker

打开终端，进到 worker 目录（把路径换成你桌面上的实际位置）：

```
cd 桌面/bohe/worker
```

安装依赖（会装 wrangler，也就是 Cloudflare 官方命令行工具）：
```
npm install
```

登录 Cloudflare（会跳出浏览器让你授权，点同意）：
```
npx wrangler login
```

部署：
```
npx wrangler deploy
```

成功后终端会打印一个地址，类似：
```
https://bohe-jubensha.你的子域名.workers.dev
```

**把这个完整地址复制下来，下一步要用。** 这就是你的后端 API 地址。

> 第一次部署 Durable Object，wrangler 可能提示要确认创建 namespace，按提示回车确认即可。

---

## 第二步：把后端地址填进前端

用文本编辑器（VS Code 最好）打开 `public/index.html`，找到这一行（在 `<script>` 开头附近）：

```js
const API = "https://bohe-jubensha.YOUR-SUBDOMAIN.workers.dev";
```

把它替换成你上一步拿到的真实地址，比如：
```js
const API = "https://bohe-jubensha.zhangsong.workers.dev";
```

保存。

---

## 第三步：部署前端到 Cloudflare Pages

进到 public 目录：
```
cd ../public
```

部署（第一次会让你给项目起个名，比如 bohe）：
```
npx wrangler pages deploy . --project-name bohe
```

成功后会打印前端地址，类似：
```
https://bohe.pages.dev
```

**这就是发给玩家的网址。** 主持人打开它建房，玩家打开它（或主持人复制的带房号链接）加入。

---

## 怎么玩（上线后）

1. 主持人手机打开 `https://bohe.pages.dev` → 点「我是主持人，建房」→ 拿到 4 位房号
2. 把房号或「复制玩家加入链接」发到微信群
3. 五个玩家各自打开链接 / 输房号 + 昵称 → 进房等待
4. 凑齐 5 人，主持人点「🎲 随机分配角色」
5. 每个玩家手机上**只会显示自己抽到的角色**（公开身份 + 私密秘密 + 目标）
6. 讨论、盘问、投票都在**微信群**里进行
7. 主持人点「下一幕」推进剧情，所有玩家的线索同步解锁

---

## 改本子 / 加内容

所有剧本内容都在 `worker/src/script.js`：
- `ROLES`：角色（数组长度 = 房间人数，想做 6 人本就加到 6 个角色）
- `STAGES`：幕（每幕的 `clues` 决定该幕解锁哪几条线索）
- `CLUES`：线索

改完重新 `cd worker` 然后 `npx wrangler deploy` 即可。前端不用动（角色内容是后端下发的）。

---

## 重要说明

**关于实时同步**：当前版本用「轮询」（前端每 1.5 秒拉一次状态）实现同步，简单可靠，对剧本杀这种慢节奏完全够用。如果以后想要更即时的体验，可以升级成 WebSocket（Durable Objects 原生支持），到时候可以再加。

**关于国内访问**：`.pages.dev` 和 `.workers.dev` 在中国大陆访问可能不稳定（取决于网络）。如果玩家都在国内且遇到打不开：
- 方案 A：绑一个你自己的境外域名（在 Cloudflare 后台 Pages/Workers 的 Custom Domains 里加），稳定很多
- 方案 B：彻底解决就得换国内云 + ICP 备案，但那就回到微信云开发那条路了

**关于费用**：免费计划完全够用。Durable Objects 已经在 Workers 免费计划开放，免费计划不收存储费，你这种自用/小范围玩法不会产生任何费用。

**关于房间清理**：房间状态存在 Durable Object 里，玩完不用管。如果想做「一局结束自动清空」，可以在 room.js 里加一个重置接口，后续需要可以再加。
